schema_version: "0.2.0"
work_id: "WORK-INVALID-SPEC"
related_specifications:
  - "SPEC-0001"
risk_profile: lean
scope:
  in_scope:
    - Exercise invalid specification validation.
  out_of_scope:
    - External changes.
authorization_envelope:
  operations:
    - operation_id: "OP-READ"
      effect_class: local-read
      decision: permitted
      authorization_requirement: bounded_work_envelope
implementation_state: not_started
reviews: []
final_gate:
  status: not_issued
human_checkpoint:
  status: not_reached
  next_required_action: Correct the fixture.
